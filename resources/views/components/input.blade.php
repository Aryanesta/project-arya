<div class="form-group">
    <label for="{{ $inputName }}">{{ $label }}</label>
    <input type="{{ $inputType }}" name="{{ $inputName }}" class="{{ $inputClass }}" placeholder="{{ $placeholder }}" id="{{ $inputName }}">
</div>
